{
  "name": "effect-modbus-rs",
  "version": "1.0.0",
  "description": "",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      }
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test ./src/ ./examples/",
    "build": "rm -rf ./dist && bun build ./index.ts --outdir ./dist --target node --external effect --external modbus-rs && tsc --noEmit false --emitDeclarationOnly --outDir ./dist --rootDir ."
  },
  "devDependencies": {
    "@effect/language-service": "^0.86.2",
    "@types/bun": "latest",
    "fallow": "^2.102.0",
    "typescript": "^6.0.3",
    "@effect/platform-bun": "^0.90.0"
  },
  "dependencies": {
    "effect": "^3.21.4",
    "modbus-rs": "^0.15.0"
  }
}
